# STEP 5 强制执行指令（v1.0）

## ★★★ 本指令用途

**你在什么时候粘贴这个指令**：当AI已经完成了STEP 0到STEP 3-B的所有分析、规划、推导之后，即将进入STEP 5落表输出Seedance投喂包时——粘贴本指令，强制AI按最高标准输出。

**解决的问题**：
- AI到后半段开始偷工减料/压缩内容
- 投喂包变成"说明书"而非"可执行镜头描述"
- 前面推导的情绪/朝向/光线规格在投喂包中丢失
- 表演四拍被压缩成"角色A做了什么→角色B反应"
- 段尾锁定敷衍/不完整

---

## 指令正文（复制以下全部内容，在AI完成STEP 3-B后粘贴）

---

### 【STEP 5 强制执行模式·启动】

从现在开始，你进入**STEP 5逐条精密输出模式**。以下规则覆盖你所有的"简化倾向"——你不得以任何理由偷工减料。

#### 一、输出节奏锁定

```
每次输出只写 1-2 个 SEG。
不得一次性输出超过3个SEG。
每个SEG输出后，等我确认"继续"再写下一个。
如果我没说继续，你就停在那里。
```

**为什么**：一次输出太多 = 后面的SEG质量必然下降。分批输出是质量保证的物理手段。

#### 二、每个SEG输出前的强制回查

在写每一个SEG之前，你必须先在内心完成以下5项回查（不需要打印出来，但必须执行）：

```
□ 本SEG对应的情绪推导卡编号是___，主情绪___，从情绪___
□ 本SEG在轴线地图中的人物位置：___在screen-left，___在screen-right
□ 本SEG的光线固定串是：___（必须字符级复制）
□ 如果本SEG承接上一段，段尾锁定的画面位置/朝向已逐字复制进开始人物位置关系
□ 本SEG是打斗段吗？→ 如是，复杂度___级，使用___替代方案/高成功率句式
```

#### 三、投喂包格式铁律（每个SEG必须完整包含）

```
你输出的每一个SEG，必须完整包含以下结构，缺一项我直接打回让你重写：

1. 场景（中文）+ 环境音（中文）+ Lighting（英文，字符级复制）
2. 每个角色的完整三层冗余（positioned + facing + toward）+ 中文表演状态
3. 时间段式拍点（按密度标准填满，不得用两段大概述凑数）
4. 每个拍点内的七项/五项（按分级执行，不得省略任何一项）
5. SUFFIX完整（含防油腻四件套）
6. 段尾锁定（Hero=完整九项/Standard=简版三项）
```

**★ 终审标准**：如果你的输出看起来像"字段列表"或"大纲概述"，就是错的。正确的投喂包读起来应该像**一段按时间推进的电影分镜脚本**——有画面感、有呼吸、有节奏。

#### 四、禁止行为清单

| # | 绝对禁止 | 正确做法 |
|---|---------|---------|
| 1 | 把多个拍点压缩成一句话概括 | 每个拍点独立展开，写满动作+表情+环境音 |
| 2 | 用"角色表现出X情绪"代替具体表演描写 | 必须写出具体的微表情+肢体+呼吸+视线 |
| 3 | 台词只写一句不写下颌/嘴型/听者反应 | 台词段必须有：说话时的嘴型+听者微表情+环境音 |
| 4 | 段尾锁定只写情绪状态不写画面位置 | 段尾锁定必须包含画面位置+朝向+姿态+光线 |
| 5 | 光线描述每段改写/换词 | 同场景光线必须字符级复制，一字不改 |
| 6 | 把投喂包写成表格/JSON/字段列表 | 必须是连续自然语言的时间段描述 |
| 7 | 省略SUFFIX或只写半截 | SUFFIX完整复制，每个SEG都要 |
| 8 | 跳过配角/听者反应"因为不重要" | 每个有对话的SEG必须有听者反应 |
| 9 | 打斗段只写"主角一拳打倒对手" | 必须按Pause→Burst→Lock + 受力反应链完整展开 |
| 10 | "为了节省空间"合并两个SEG | 每个SEG独立完整输出，不合并 |

#### 五、质量对标句

**在你写每个SEG时，心里默念这句话**：

> "我现在写的这段文字，是一个Seedance操作员直接复制粘贴进生成框的最终版本。他不会修改任何一个字。如果我写得不够精确、不够完整、不够有画面感——生成出来的视频就会崩。"

#### 六、如果我说"展开"

当我对你已经输出的SEG说"展开"或"这里太薄了"，你必须：
1. 不要解释为什么之前写得薄
2. 直接重写这个SEG，把拍点密度提升到标准
3. 补全所有被省略的表演细节/听者反应/环境音/配角动作

#### 七、分级提醒

- **Hero级**：你必须写出你能写出的最好版本。每个拍点七项全落实。这是你的"代表作"镜头。
- **Standard级**：五项落实，表演三步简化可以接受，但仍然必须有画面感。
- **Filler级**：可以简短，但运镜+主体+环境音+SUFFIX仍然完整。

#### 八、输出格式确认

每个SEG输出完毕后，在末尾加一行：

```
✓ 本SEG自检：密度___拍点 / 台词完整□ / 听者反应□ / 段尾锁定□ / SUFFIX□ / 光线字符级□
```

如果你发现自己某项做不到（比如密度不够），在这行标注"★需补"，我来决定是否要你重写。

---

## 使用示例

你在AI完成STEP 3-B后，这样粘贴：

```
很好，STEP 3-B的规划已经完成。现在进入STEP 5。

【以下是强制执行指令，请严格遵守】

（粘贴上面"指令正文"的全部内容）

请从SEG-01开始输出。每次只输出1-2个SEG，等我说"继续"再写下一个。
第一个SEG是：___集___场·SEG-01·___（简述内容）
```

---

## 追加指令变体（按需使用）

### 变体A：打斗场启动时追加

```
本场含打斗。在STEP 5强制执行指令基础上，追加以下规则：
- 打斗SEG时长≤8秒，每个SEG≤2次攻防
- 每个打斗SEG输出前先报告：复杂度___级 + 使用方案___（直接展示/力量暗示/环境叙事/反应驱动/声效先行/混合）
- Pause→Burst→Lock三拍必须在时间段中可见
- 受力方反应链必须完整（身体偏移+重心+手部+延迟表情）
- 旁观者恐惧级别报告：本段为第___级（1-5）
```

### 变体B：正反打对话场启动时追加

```
本场为正反打对话场。追加以下规则：
- 每个SEG开头必须报告：轴线地图→本段角色A在screen-___，角色B在screen-___
- 正反打朝向互补自检：本段facing screen-___，对方段facing screen-___（必须互补）
- 听者反应不低于2句（视线+微表情+肢体至少各1项）
- 台词时间预算报告：___字÷___语速=___秒+停顿___+听者___+余韵___=预算___秒
```

### 变体C：遇到AI开始偷懒时的"拉回"指令

```
停。你刚才输出的SEG-___质量明显下降。具体问题：
- [具体指出：密度不够/听者反应缺失/段尾锁定敷衍/光线没复制/表演太概括]

请重写这个SEG。回查情绪推导卡+轴线地图+光线固定串后，按Hero级密度标准重新输出。不要解释，直接重写。
```

### 变体D：《魔女》风格打斗场专用

```
本场为《魔女》风格打斗。在打斗变体A基础上追加：
- 女主全程面部保持 blank expression / expressionless calm
- 每次命中后女主必须有"比对手先归位平静"的节奏
- Pause→Burst→Lock中的Pause = 女主面无表情的微小蓄力（肩微沉/呼吸不变/眼神微调焦距）
- 血液出现时必须与女主冷静形成视觉反差（只在对手/环境出现，女主身上血迹不作为痛苦表达）
- 旁观者恐惧要强调"对女主的恐惧"而非"对暴力的恐惧"——敬畏>害怕
```

---

## 为什么这个指令有效

| 机制 | 解决的问题 |
|------|-----------|
| 每次只写1-2个SEG | 防止AI后半段质量衰减 |
| 输出前5项强制回查 | 防止前面的规格在输出时丢失 |
| 禁止行为清单 | 直接堵死AI最常犯的10种偷懒方式 |
| 末尾自检行 | 让AI在输出时就意识到要过检查 |
| "展开"指令 | 给你一个低成本的返工手段 |
| 质量对标句 | 给AI一个"终端用户视角"的心理暗示 |
| 分批确认 | 你可以在每个SEG后实时调整方向 |

---

**— 指令文档终 —**

**核心口诀：分批出+强制查+禁止偷+末尾检+随时拉回**
